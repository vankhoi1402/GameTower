public enum UIPanelType
{
    Prepare,    // Thay cho MainMenu nếu đây là màn chờ chuẩn bị vào trận
    Playing,    // Giao diện HUD khi đang chơi (Máu, điểm, nút Pause)
    Paused,     // Màn hình Menu khi bấm dừng game
    EndGame      // Màn hình thua cuộc
}